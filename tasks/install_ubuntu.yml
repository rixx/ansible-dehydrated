---
- name: Ensure all dehydrated dependencies are installed
  ansible.builtin.apt:
    pkg: "{{ item }}"
    state: present
  with_items:
    - openssl
    - curl
    - sed
    - grep
    - coreutils
  tags:
    - apt
    - dehydrated
